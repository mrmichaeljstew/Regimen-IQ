NEXT_PUBLIC_APPWRITE_PROJECT_ID=regimen-iq
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://sfo.cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_DATABASE_ID=regimen-iq-db

# API Key for setup script (server-side only, never expose in client)
# Create in Appwrite Console: Settings > API Keys > Create API Key
# Required scopes: databases.read, databases.write, collections.read, collections.write
APPWRITE_API_KEY=your_api_key_here
